# CLAUDE.md · 项目真相文件

本文件是 Claude Code 在本项目中的最高优先级规则。与任何其他建议冲突时，以本文件为准。
来源：`PDF_Convert_KICKOFF.md` §1 与 §8。

---

## 1. 硬约束（不可违反）

| # | 约束 | 具体要求 |
|---|---|---|
| C1 | 零服务器 | 不写任何后端代码，不调用任何外部接口。页面加载后只允许请求同源静态资源（worker 文件、字体映射文件）。 |
| C2 | 零运营成本 | 只用免费静态托管。 |
| C3 | 许可证白名单 | 只允许 MIT / Apache-2.0 / BSD / ISC。禁止 GPL、AGPL（例如 MuPDF、Ghostscript）。**新增任何依赖前，先在 `DECISIONS.md` 写明许可证并征得 Leo 同意。** |
| C4 | 无第三方脚本 | 无统计、无广告、无外部字体、无 CDN。所有资源随站点一起打包。 |
| C5 | 操作边界 | 只在本项目目录内读写。不删除、不移动项目目录以外的任何文件。不执行 `git push --force`，不修改全局配置。 |
| C6 | 用户文件不入库 | 真实 PDF 样本只放 `fixtures/manual/`，该目录已写入 `.gitignore`。 |

### 约束的落地检查点

- 提 PR / 收尾前，`grep` 构建产物里有没有 `http://` `https://` 开头的外部地址。
- `package.json` 每次变动，都要能说出新依赖的许可证。
- 阶段 4 的 CSP 元标签是 C1/C4 的最后一道防线，不得为了省事放宽到 `*`。

---

## 2. 工作规则

1. **一次只做一个阶段**；阶段结束后停下，列出改动、测试结果和待验收项，等 Leo 验收。
2. 每次会话结束更新 `HANDOFF.md`：完成了什么、卡在哪里、下一步是什么。
3. 遇到本文档没覆盖的**产品或架构选择**：列出 2–3 个选项和推荐意见，等 Leo 决定。
4. **小的实现细节**自行决定，并简短记入 `DECISIONS.md`。
5. 不确定时先问，不要猜。
6. **提交信息用英文；项目文档用中文。**

---

## 3. 相关文件

- `PLAN.md` —— 分阶段计划与当前进度
- `DECISIONS.md` —— 已定决策与实现细节记录
- `HANDOFF.md` —— 会话交接
- `PDF_Convert_KICKOFF.md`（在 `~/Downloads/`，未入库）—— 原始开工文档
