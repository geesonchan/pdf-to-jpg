{
  "name": "pdf-to-jpg",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Convert each page of a PDF into a JPG image, entirely in your browser.",
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
